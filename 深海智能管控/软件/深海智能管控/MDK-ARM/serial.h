//#ifndef SERIAL_H
//#define SERIAL_H

//#include <stdint.h>  // 如果需要使用标准整数类型（如 uint8_t、uint16_t 等）
//#include <stddef.h>  // 如果需要使用 size_t 类型

//// 如果需要使用 HAL 库或其他特定平台的库，可以包含相关头文件
//// #include "stm32f4xx_hal.h"  // 根据您的实际情况调整

//// 串口初始化函数
//void serial_init(void);

//// 从串口读取一行数据的函数
//void serial_readline(char *buffer, size_t max_length);

//// 串口写入函数
//void serial_write(const char *data);

//// 从串口读取一个字符的函数
//char serial_getchar(void);  // 添加这行以声明serial_getchar函数

//#endif /* SERIAL_H */
